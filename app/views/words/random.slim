- if @word
  .more-padding
    h3 = t("word.choose_word")
    = form_for @word_search, :html => {:role => "form"} do |f|
      = f.text_field :name, :class => "form-control", :id => "word_search", :placeholder => "Search for a word", type: "search"
    h3 or
    h2 = @word.name
    hr
    = form_for @word, :html => {:role => "form", :url => word_path(@word.id), :method => :put}  do |f|
      - @tags.each do |tag|
        div.radio
          label
           = f.radio_button :tag_id, tag.id
           = tag.name
      hr
      div.center-aligned
        = link_to "Skip", root_path, :class => "btn btn-danger"
        = f.submit "Save", :class => "btn btn-success"
- else
  p.bg-info.padding15
    |All words are tagged by users. If you want to review words. click 
    a href="/review" Review

